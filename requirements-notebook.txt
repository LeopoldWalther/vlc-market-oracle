# VS Code executes notebooks through an IPython kernel; a Jupyter server is not required locally.
ipykernel>=6.29,<8
playwright>=1.62,<2
playwright-stealth>=2.0.3,<3
selenium>=4.25,<5
# undetected-chromedriver still imports distutils, which setuptools provides on Python 3.12.
setuptools>=68
undetected-chromedriver>=3.5.5,<4